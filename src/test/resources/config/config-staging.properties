# ── PayShack API Configuration (staging environment) ────────────────────────
# Usage: mvn test -Denv=staging

apibaseUrl=https://staging-api.payshack.in
clientId=REPLACE_WITH_STAGING_CLIENT_ID
secreteKey=REPLACE_WITH_STAGING_SECRET_KEY
encryptionkey=REPLACE_WITH_STAGING_ENCRYPTION_KEY

statusOrderId=your_staging_order_id_here
